CREATE TABLE party_votes (
    voter_id INT PRIMARY KEY,
    party_a INT DEFAULT 0,
    party_b INT DEFAULT 0,
    party_c INT DEFAULT 0
);

INSERT INTO party_votes (voter_id) VALUES (1);
INSERT INTO party_votes (voter_id) VALUES (2);
INSERT INTO party_votes (voter_id) VALUES (3);

UPDATE party_votes
SET party_a = party_a + 1
WHERE voter_id = 1;

UPDATE party_votes
SET party_b = party_b + 1
WHERE voter_id = 2;

UPDATE party_votes
SET party_c = party_c + 1
WHERE voter_id = 3;


SELECT 
SUM(party_a) AS total_party_a,
SUM(party_b) AS total_party_b,
SUM(party_c) AS total_party_c
FROM party_votes;